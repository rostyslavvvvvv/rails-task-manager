<h1> Task <%= @task.title %> </h1>
<p>Details: <%= @task.details %></p>

<p>
  <%= link_to 'Delete',
   task_path(@task),
   data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }
  %>
</p>

<p> <%= link_to 'Show the list of Tasks', tasks_path %> </p>
